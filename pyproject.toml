[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "depression"
version = "1.0.0"
description = "AI-powered CLI agent for depression.ai - your intelligent terminal assistant"
readme = "README.md"
license = {text = "ISC"}
authors = [{name = "rnaveenpatil", email = "rnaveenpatil@example.com"}]
keywords = ["ai", "cli", "agent", "depression"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: ISC License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.25.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "tiktoken>=0.5.0",
    "rich>=13.0",
    "prompt-toolkit>=3.0",
    "textual>=0.80.0",
]

[project.optional-dependencies]
tui = [
    "textual>=0.80.0",
    "textual-dev>=1.0.0",
]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.1.0",
    "mypy>=1.0",
]

[project.scripts]
depression = "agent.main:main"
depression_ai = "agent.main:main"
depression-tui = "agent.tui.run:main"

[project.urls]
Homepage = "https://github.com/rnaveenpatil/depression.ai"
Repository = "https://github.com/rnaveenpatil/depression.ai"
Issues = "https://github.com/rnaveenpatil/depression.ai/issues"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
